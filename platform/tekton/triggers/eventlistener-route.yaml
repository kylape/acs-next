apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: forgejo-webhook
  namespace: acs-next
spec:
  to:
    kind: Service
    name: el-forgejo-listener
    weight: 100
  port:
    targetPort: http-listener
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
