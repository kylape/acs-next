---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusterstackroxpolicies.policy.stackrox.io
spec:
  group: policy.stackrox.io
  names:
    kind: ClusterStackroxPolicy
    listKind: ClusterStackroxPolicyList
    plural: clusterstackroxpolicies
    shortNames:
    - csrxp
    - csp
    singular: clusterstackroxpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.policyName
      name: Policy Name
      type: string
    - jsonPath: .spec.severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=="AcceptedByAdmissionControl")].status
      name: Accepted
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterStackroxPolicy is the schema for cluster-scoped policies
          in secured clusters
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterStackroxPolicySpec defines the desired state of ClusterStackroxPolicy
            properties:
              categories:
                items:
                  type: string
                minItems: 1
                type: array
              description:
                pattern: ^[^\$]{0,800}$
                type: string
              disabled:
                type: boolean
              enforcementActions:
                items:
                  description: EnforcementAction defines enforcement actions for policy
                    violations
                  enum:
                  - UNSET_ENFORCEMENT
                  - SCALE_TO_ZERO_ENFORCEMENT
                  - UNSATISFIABLE_NODE_CONSTRAINT_ENFORCEMENT
                  - KILL_POD_ENFORCEMENT
                  - FAIL_KUBE_REQUEST_ENFORCEMENT
                  - FAIL_DEPLOYMENT_CREATE_ENFORCEMENT
                  - FAIL_DEPLOYMENT_UPDATE_ENFORCEMENT
                  type: string
                type: array
              eventSource:
                description: EventSource defines which events should trigger policy
                  execution
                enum:
                - NOT_APPLICABLE
                - DEPLOYMENT_EVENT
                - AUDIT_LOG_EVENT
                - NODE_EVENT
                type: string
              exclusions:
                items:
                  description: Exclusion defines what should be excluded from policy
                  properties:
                    deployment:
                      description: Deployment specifies deployment-based exclusions
                      properties:
                        name:
                          description: Name is the deployment name to exclude
                          type: string
                        scope:
                          description: Scope narrows the exclusion to specific namespaces
                          properties:
                            namespace:
                              description: Namespace is a direct namespace name to
                                target
                              type: string
                            namespaceSelector:
                              description: NamespaceSelector is a Kubernetes label
                                selector for namespaces
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            workloadSelector:
                              description: WorkloadSelector is a Kubernetes label
                                selector for workloads
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      type: object
                    expiration:
                      description: Expiration is when this exclusion expires
                      format: date-time
                      type: string
                    image:
                      description: Image specifies image-based exclusions
                      properties:
                        name:
                          description: Name is the image name to exclude (supports
                            wildcards)
                          type: string
                      type: object
                    name:
                      description: Name is a descriptive name for this exclusion
                      type: string
                    workloadSelector:
                      description: WorkloadSelector excludes workloads matching this
                        label selector
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              lifecycleStages:
                items:
                  description: LifecycleStage defines the stage in the lifecycle where
                    a policy applies
                  enum:
                  - DEPLOY
                  - RUNTIME
                  type: string
                minItems: 1
                type: array
              mitreAttackVectors:
                items:
                  description: MitreAttackVectors defines MITRE ATT&CK framework mapping
                  properties:
                    tactic:
                      description: Tactic is the MITRE ATT&CK tactic
                      type: string
                    techniques:
                      description: Techniques are the MITRE ATT&CK techniques
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              notifiers:
                items:
                  type: string
                type: array
              policyName:
                pattern: ^[^\n\r\$]{5,128}$
                type: string
              policySections:
                items:
                  description: PolicySection defines a section of policy criteria
                  properties:
                    policyGroups:
                      description: PolicyGroups is the set of policy groups that make
                        up this section
                      items:
                        description: PolicyGroup defines a group of policy criteria
                        properties:
                          booleanOperator:
                            description: BooleanOperator determines if the values
                              are combined with OR or AND
                            enum:
                            - OR
                            - AND
                            type: string
                          fieldName:
                            description: FieldName defines which field on a deployment
                              or image this PolicyGroup evaluates
                            type: string
                          negate:
                            description: Negate determines if the evaluation of this
                              PolicyGroup is negated
                            type: boolean
                          values:
                            description: Values is the list of values for the specified
                              field
                            items:
                              description: PolicyValue defines a single policy value
                              properties:
                                value:
                                  description: Value is the string value
                                  type: string
                              type: object
                            type: array
                        required:
                        - fieldName
                        type: object
                      type: array
                    sectionName:
                      description: SectionName is a user-friendly name for this section
                      type: string
                  required:
                  - policyGroups
                  type: object
                minItems: 1
                type: array
              rationale:
                type: string
              remediation:
                type: string
              scope:
                items:
                  description: Scope defines scope for cluster-scoped policies
                  properties:
                    namespace:
                      description: Namespace is a direct namespace name to target
                      type: string
                    namespaceSelector:
                      description: NamespaceSelector is a Kubernetes label selector
                        for namespaces
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    workloadSelector:
                      description: WorkloadSelector is a Kubernetes label selector
                        for workloads
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                  type: object
                type: array
              severity:
                enum:
                - UNSET_SEVERITY
                - LOW_SEVERITY
                - MEDIUM_SEVERITY
                - HIGH_SEVERITY
                - CRITICAL_SEVERITY
                type: string
            required:
            - categories
            - lifecycleStages
            - policyName
            - policySections
            - severity
            type: object
          status:
            description: ClusterStackroxPolicyStatus defines the observed state of
              ClusterStackroxPolicy
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastEvaluated:
                format: date-time
                type: string
              localPolicyId:
                type: string
              recentViolations:
                items:
                  description: RecentViolation records a single policy violation event
                  properties:
                    enforcementAction:
                      type: string
                    message:
                      type: string
                    resource:
                      description: ViolationResource identifies the resource that
                        violated a policy
                      properties:
                        containerName:
                          type: string
                        imageName:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    source:
                      type: string
                    timestamp:
                      format: date-time
                      type: string
                  required:
                  - message
                  - resource
                  - source
                  - timestamp
                  type: object
                maxItems: 20
                type: array
              violationMetrics:
                description: ClusterScopedViolationMetrics tracks violations for cluster-scoped
                  policies
                properties:
                  activeViolations:
                    format: int32
                    type: integer
                  lastViolationTime:
                    format: date-time
                    type: string
                  totalViolations:
                    format: int32
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
